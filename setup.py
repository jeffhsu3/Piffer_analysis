import os, sys, glob

name = 'pyfreewas'

import numpy
import pysam

from distutils.core import setup
from distutils.extension import Extension

metadata = {'name': name,
            'version': version,
        }
